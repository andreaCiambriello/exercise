const person = {
    firstName: 'Mario',
    lastName: 'Rossi',
    age: 25
  }
  //keys
  let keys = Object.keys(person);
  console.log(keys);
  //values
  let values = Object.values(person);
  console.log(values);
  //key/value pairs
  let entries = Object.entries(person);
  console.log(entries);